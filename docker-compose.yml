version: "3.8"

services:
  api:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    working_dir: /code/
    volumes:
      - .:/code
    command: sh -c "python app"
    environment:
      PYTHONPATH: "/code/"
